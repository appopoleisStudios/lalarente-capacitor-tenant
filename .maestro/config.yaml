# Maestro E2E — visible UI automation (simulator / device)
# Credentials: .maestro/.env (gitignored) — passed via scripts/run-e2e*.sh --env flags.
# Do NOT set empty env defaults here; they override CLI credentials in subflows.

appId: com.lalarente.app
