# Open tenant profile via Profile tab (no deep links).
appId: com.lalarente.app
---
- tapOn:
    id: "tab-profile"

- extendedWaitUntil:
    visible:
      id: "tenant-profile-title"
    timeout: 20000
