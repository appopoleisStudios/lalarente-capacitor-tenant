# Self-contained tenant dashboard E2E test
# Uses re-usable login-tenant subflow which handles both cached session and fresh login.
# Navigation uses tab / quick-action taps — no deep links.
appId: com.lalarente.app
---
- runFlow: ../subflows/login-tenant.yaml

- runFlow: ../subflows/tap-tenant-maintenance.yaml

- runFlow: ../subflows/tap-tenant-lala-ai.yaml
