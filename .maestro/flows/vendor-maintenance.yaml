# Vendor maintenance — open maintenance requests screen via tab (no deep links).
appId: com.lalarente.app
---
- runFlow: ../subflows/launch-app-warm.yaml
- runFlow: ../subflows/login-vendor.yaml

- runFlow: ../subflows/tap-vendor-maintenance.yaml

- assertVisible:
    text: "Maintenance Requests"
    optional: true
